<p id="notice"><%= notice %></p>

<b><h2 style="color:yellow; text-align:center" class="column"> <%= @product.brand %> : <%= @product.name %> </h2></b>

<div class="row">
  <center><div class="large-6 columns"><%= image_tag @product.image_url %></br></br></br>
  		<div>
  		<%= button_to line_items_path(:product_id => @product) do %>
  		 		<center><div class="button round column">
            Add to Cart
          </div></center>
          <% end %>
        </div>
  </div></center>
  <div class="large-6 columns" style="color:white; text-align:justify"><%= @product.long_description %></div>
</div>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
